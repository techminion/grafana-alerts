FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY sites ./sites
RUN python -m pip install --no-cache-dir .

RUN useradd --create-home --uid 10001 proxy
USER proxy
EXPOSE 8080

CMD ["grafana-alerts-proxy"]
